import { NotificationDatePipe } from './notification-date.pipe';

describe('NotificationDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationDatePipe();
    expect(pipe).toBeTruthy();
  });
});
